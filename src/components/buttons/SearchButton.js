import React, { Component } from "react";
// import "./SearchButton.css";

class SearchButton extends Component {
  render() {
    return (
      <div className="SearchButton">
        <button>SEARCH RECIPES FROM THIS INGREDIENTS</button>
      </div>
    );
  }
}

export default SearchButton;
